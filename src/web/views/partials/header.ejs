<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <meta property="og:title" content="<%= bot && bot.username ? (bot.username + ' - Inicio') : 'Dashboard' %>">
    <meta property="og:description" content="Gestiona tu economía, niveles y servidores con <%= bot && bot.username ? bot.username : 'tu bot' %>">
    <meta property="og:image" content="<%= (bot && bot.displayAvatarURL) ? bot.displayAvatarURL({format: 'png', size: 512}) : 'https://cdn.discordapp.com/embed/avatars/0.png' %>">
    <meta property="og:theme-color" content="#ff4757">
    <link rel="icon" href="<%= bot.displayAvatarURL({format: 'png'}) %>">

    <title><%= bot.username %> - Inicio</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>

    <nav class="top-navbar">
        <div class="nav-left">
            <img src="<%= (bot && bot.displayAvatarURL) ? bot.displayAvatarURL({format: 'png'}) : 'https://cdn.discordapp.com/embed/avatars/0.png' %>" class="nav-logo">
            <a href="/" class="nav-brand"><%= bot && bot.username ? bot.username : 'Mi Bot' %></a>
        </div>

        <div class="nav-center">
            <a href="/comandos" class="nav-link <%= (typeof currentPath !== 'undefined' && currentPath.startsWith('/comandos')) ? 'active' : '' %>">Comandos</a>
            <a href="/dashboard" class="nav-link <%= (typeof currentPath !== 'undefined' && currentPath.startsWith('/dashboard')) ? 'active' : '' %>">Dashboard</a>
            <a href="/leaderboard" class="nav-link <%= (typeof currentPath !== 'undefined' && currentPath.startsWith('/leaderboard')) ? 'active' : '' %>">Leaderboard</a>
        </div>

        <div class="nav-right">
            <a href="https://discord.com/oauth2/authorize?client_id=1190705096963657858&permissions=8&scope=bot%20applications.commands" target="_blank" class="nav-action">
                <i class="fab fa-discord"></i> Invitar
            </a>
            
            <% if(user) { %>
                <div class="user-profile" onclick="window.location.href='/dashboard'">
                    <img src="<%= user && user.avatar ? ('https://cdn.discordapp.com/avatars/' + user.id + '/' + user.avatar + (user.avatar.startsWith && user.avatar.startsWith('a_') ? '.gif' : '.png')) : 'https://cdn.discordapp.com/embed/avatars/0.png' %>" class="user-avatar" onerror="this.src='https://cdn.discordapp.com/embed/avatars/0.png'">
                    <span class="user-name"><%= user.username %></span>
                </div>
                <a href="/logout" class="nav-action" style="font-size:12px; color:#ed4245;">Salir</a>
            <% } else { %>
                <a href="/login" class="nav-action">
                    <i class="fas fa-sign-in-alt"></i> Entrar
                </a>
            <% } %>
        </div>
    </nav>
