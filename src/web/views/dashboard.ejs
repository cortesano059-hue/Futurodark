<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <meta property="og:title" content="<%= (bot && bot.username) ? (bot.username + ' - Dashboard') : 'Dashboard' %>">
    <meta property="og:theme-color" content="#ff4757">
    <link rel="icon" href="<%= (bot && bot.displayAvatarURL) ? bot.displayAvatarURL({format: 'png'}) : 'https://cdn.discordapp.com/embed/avatars/0.png' %>">

    <title><%= (bot && bot.username) ? (bot.username + ' - Dashboard') : 'Dashboard' %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        /* --- PALETA DE COLORES Y CUERPO (COPIA EXACTA DE INDEX.EJS) --- */
        :root {
            --bg-container: #1e2124; 
            --bg-element: rgba(0, 0, 0, 0.2); 
            --border: #2f3136;
            --accent: #ff4757; 
            --text-main: #ffffff;
            --text-dim: #b9bbbe;
            --navbar-bg: #141619;
        }

        * { box-sizing: border-box; }

        body {
            background-image: url('https://wallpaperaccess.com/full/1155017.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            box-shadow: inset 0 0 0 2000px rgba(15, 17, 20, 0.92);
            background-color: #0f1114;
            color: var(--text-main);
            font-family: 'Segoe UI', 'Roboto', sans-serif;
            margin: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* --- NAVBAR: COPIA LITERAL DE TU INDEX.EJS --- */
        .top-navbar {
            width: 100%;
            height: 60px; 
            background-color: var(--navbar-bg); 
            border-bottom: 1px solid #000000;
            display: flex; 
            align-items: center; 
            justify-content: space-between; 
            padding: 0 25px;
            position: sticky;
            top: 0; 
            z-index: 1000; 
            font-size: 14px;
        }

        .nav-left { display: flex; align-items: center; gap: 12px; }
        .nav-logo { width: 32px; height: 32px; border-radius: 50%; }
        .nav-brand { font-weight: 700; font-size: 18px; color: white; text-decoration: none; }

        .nav-center { 
            display: flex; 
            gap: 25px; 
            position: absolute; 
            left: 50%; 
            transform: translateX(-50%); 
        }
        .nav-link { 
            color: var(--text-dim); 
            text-decoration: none; 
            font-weight: 500; 
            transition: color 0.2s; 
        }
        .nav-link:hover { color: white; }
        .nav-link.active { color: var(--accent); font-weight: bold; }

        .nav-right { display: flex; align-items: center; gap: 20px; }
        .nav-action { 
            display: flex; 
            align-items: center; 
            gap: 6px; 
            color: var(--text-dim); 
            text-decoration: none;
            font-weight: 600; 
            transition: 0.2s; 
        }
        .nav-action:hover { color: white; }
        
        .user-profile { 
            display: flex;
            align-items: center; 
            gap: 8px; 
            padding-left: 15px; 
            border-left: 1px solid var(--border); 
            cursor: pointer;
        }
        .user-avatar { width: 28px; height: 28px; border-radius: 50%; }
        .user-name { font-weight: 600; color: white; }

        /* --- CONTENEDORES --- */
        .main-wrapper { flex: 1; display: flex; flex-direction: column; align-items: center; padding: 40px 20px; }
        
        .glass-panel {
            width: 100%;
            max-width: 950px;
            background: rgba(30, 33, 36, 0.8);
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 45px;
            margin-bottom: 35px;
            text-align: center;
            position: relative;
            box-shadow: 0 15px 35px rgba(0,0,0,0.5);
            backdrop-filter: blur(10px);
        }

        .glass-panel::before {
            content: '';
            position: absolute; top: 0; left: 0; right: 0; height: 3px; background: var(--accent);
            box-shadow: 0 0 15px var(--accent);
        }

        .section-title { font-size: 22px; font-weight: 800; margin-bottom: 40px; text-transform: uppercase; letter-spacing: 2px; }

        /* --- GRID ESTILO UNBELIEVABOAT --- */
        .guilds-grid { display: flex; flex-wrap: wrap; justify-content: center; gap: 35px; }
        .guild-card { position: relative; text-decoration: none; }

        .icon-container {
            width: 120px;
            height: 120px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.3s ease;
        }

        /* Hover Glassmorphism */
        .guild-card:hover .icon-container {
            background: rgba(255, 255, 255, 0.1); 
            border-color: rgba(255, 255, 255, 0.4);
            transform: translateY(-8px) scale(1.05);
        }

        .guild-img {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            background-size: cover;
            background-position: center;
            background-color: #2f3136;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 20px;
            color: white;
            box-shadow: 0 8px 20px rgba(0,0,0,0.5);
        }

        /* TOOLTIP BLANCO PURO AL PASAR EL RATÓN */
        .tooltip {
            position: absolute;
            bottom: -35px;
            left: 50%;
            transform: translateX(-50%);
            background: #fff;
            color: #000; 
            padding: 5px 12px;
            border-radius: 4px;
            font-size: 11px;
            font-weight: 800;
            opacity: 0;
            transition: 0.2s;
            pointer-events: none;
            white-space: nowrap;
        }

        .guild-card:hover .tooltip { opacity: 1; bottom: -45px; }

        .plus-badge {
            position: absolute;
            top: 5px;
            right: 5px;
            background: var(--accent);
            width: 26px;
            height: 26px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 11px;
            border: 3px solid #1e2124;
            z-index: 5;
        }
    </style>
</head>
<body>

    <nav class="top-navbar">
        <div class="nav-left">
            <img src="<%= (bot && bot.displayAvatarURL) ? bot.displayAvatarURL({format: 'png'}) : 'https://cdn.discordapp.com/embed/avatars/0.png' %>" class="nav-logo">
            <a href="/" class="nav-brand"><%= (bot && bot.username) ? bot.username : 'Mi Bot' %></a>
        </div>

        <div class="nav-center">
            <a href="/comandos" class="nav-link">Comandos</a>
            <a href="/dashboard" class="nav-link active">Dashboard</a>
            <a href="/leaderboard" class="nav-link">Leaderboard</a>
        </div>

        <div class="nav-right">
            <% if (bot && bot.id) { %>
                <a href="https://discord.com/oauth2/authorize?client_id=<%= bot.id %>&permissions=8&scope=bot%20applications.commands" target="_blank" class="nav-action">
                    <i class="fab fa-discord"></i> Invitar
                </a>
            <% } %>
            
            <% if(user) { %>
                <div class="user-profile" onclick="window.location.href='/dashboard'">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" class="user-avatar">
                    <span class="user-name"><%= user.username %></span>
                </div>
                <a href="/logout" class="nav-action" style="font-size:12px; color:#ed4245; margin-left: 10px;">Salir</a>
            <% } %>
        </div>
    </nav>

    <div class="main-wrapper">
        <div class="glass-panel">
            <div class="section-title">Servidores</div>
            <div class="guilds-grid">
                <% mutualGuilds.forEach(g => { %>
                    <a href="/dashboard/<%= g.id %>" class="guild-card">
                        <div class="icon-container">
                            <div class="guild-img" id="mutual-<%= g.id %>">
                                <% if(!g.icon) { %><%= g.acronym %><% } %>
                            </div>
                        </div>
                        <div class="tooltip"><%= g.name %></div>
                    </a>
                    <% if(g.icon) { %>
                        <script>document.getElementById('mutual-<%= g.id %>').style.backgroundImage = "url('<%= g.icon %>')";</script>
                    <% } %>
                <% }) %>
            </div>
        </div>

        <div class="glass-panel">
            <div class="section-title" style="font-size: 16px; color: var(--text-dim);">Invitar al Bot</div>
            <div class="guilds-grid">
                <% inviteGuilds.forEach(g => { %>
                    <% if (bot && bot.id) { %>
                    <a href="https://discord.com/oauth2/authorize?client_id=<%= bot.id %>&permissions=8&scope=bot%20applications.commands&guild_id=<%= g.id %>" target="_blank" class="guild-card">
                    <% } else { %>
                    <a href="/" class="guild-card">
                    <% } %>
                        <div class="icon-container">
                            <div class="guild-img" id="invite-<%= g.id %>" style="filter: grayscale(0.5); opacity: 0.8;">
                                <% if(!g.icon) { %><%= g.acronym %><% } %>
                            </div>
                            <div class="plus-badge"><i class="fas fa-plus"></i></div>
                        </div>
                        <div class="tooltip">Invitar a <%= g.name %></div>
                    </a>
                    <% if(g.icon) { %>
                        <script>document.getElementById('invite-<%= g.id %>').style.backgroundImage = "url('<%= g.icon %>')";</script>
                    <% } %>
                <% }) %>
            </div>
        </div>
    </div>

</body>
</html>