<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Economía en <%= guild.name %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* Mismos estilos base */
        :root { --accent: #ff4757; --bg-card: #1e2124; --text-dim: #b9bbbe; }
        body {
            background: #0f1114 url('https://wallpaperaccess.com/full/1155017.jpg') center/cover fixed;
            color: white; font-family: 'Segoe UI', sans-serif; margin: 0; min-height: 100vh;
            box-shadow: inset 0 0 0 2000px rgba(15, 17, 20, 0.95);
        }
        .navbar { height: 60px; background: #141619; display: flex; align-items: center; justify-content: space-between; padding: 0 25px; border-bottom: 1px solid #000; }
        .back-btn { color: var(--text-dim); text-decoration: none; font-weight: bold; font-size: 14px; }
        .back-btn:hover { color: white; }

        .wrapper { display: flex; justify-content: center; padding-top: 60px; }
        
        .eco-card {
            background: var(--bg-card); padding: 40px; border-radius: 8px; border: 1px solid #2f3136;
            width: 100%; max-width: 500px; text-align: center; position: relative;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }
        .eco-card::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px; background: var(--accent); }

        .server-icon { width: 100px; height: 100px; border-radius: 50%; margin-bottom: 20px; border: 4px solid var(--bg-card); box-shadow: 0 0 0 2px var(--accent); }
        
        .money-display {
            background: rgba(0,0,0,0.2); border-radius: 8px; padding: 20px; margin-top: 30px;
            display: flex; justify-content: space-around;
        }
        .val { font-size: 22px; font-weight: bold; font-family: monospace; }
        .lbl { font-size: 11px; text-transform: uppercase; color: var(--text-dim); font-weight: bold; margin-bottom: 5px; display: block; }
    </style>
</head>
<body>

    <nav class="navbar">
        <a href="/dashboard" class="back-btn">⬅ Volver al Dashboard</a>
        <div style="font-weight:bold;"><%= user.username %></div>
    </nav>

    <div class="wrapper">
        <div class="eco-card">
            
            <% if(guild.icon) { %>
                <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" class="server-icon">
            <% } else { %>
                <div class="server-icon" style="background:#333; display:flex; align-items:center; justify-content:center; margin: 0 auto 20px auto; font-size:30px;">
                    <%= guild.name.substring(0,2) %>
                </div>
            <% } %>

            <h1><%= guild.name %></h1>
            <p style="color:#b9bbbe;">Tu balance actual en este servidor</p>

            <div class="money-display">
                <div>
                    <span class="lbl"><i class="fas fa-wallet"></i> Cartera</span>
                    <div class="val" style="color:#2ecc71">$<%= economy.money.toLocaleString() %></div>
                </div>
                <div style="width:1px; background:#ffffff20;"></div>
                <div>
                    <span class="lbl"><i class="fas fa-university"></i> Banco</span>
                    <div class="val" style="color:#f1c40f">$<%= economy.bank.toLocaleString() %></div>
                </div>
            </div>

        </div>
    </div>

</body>
</html>